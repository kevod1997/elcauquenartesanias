# Fase 4 — Login y sesión

**Estado:** preparada el 2026-09-25; falta la implementación (prompt 6.2).
**Definición:** [PRD, fase 4](../PRD.md#fase-4--login-y-sesión). Aplica F4-D1 a F4-D6, sin repetirlas
acá. F4-D1 reemplaza el rewrite de D2 por redirects.

## Preparación

- Dependencias: la fase 2 está cerrada.
- Sin términos nuevos para el `CONTEXT.md` del backend: la UI dice "integrante" (no "usuario" ni
  "admin"), `owner` y `editor`, e "Iniciá sesión", como los mensajes del contrato.
- El `.env` local del backend ya tiene `ADMIN_ORIGIN` y `RESET_PASSWORD_URL` apuntando a
  `localhost:4321`; la parte local de "Hacer (usuario)" está hecha.

## Implementado

Pendiente.

## Validaciones ejecutadas

Pendiente. El plan local está en F4-D6.
